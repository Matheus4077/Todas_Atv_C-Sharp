using System;
using System.Globalization;

namespace FunçoesMate
{
    internal class Program
    {
        static void Main(string[] args)
        {
            delta = Math.Pow(b, 2.0) - 4*a*c;

            x1 = (-b + Math.Sqrt(delta)) / (2.0*a);
            x2 = (-b - Math.Sqrt(delta)) / (2.0 * a);
        }
    }
}